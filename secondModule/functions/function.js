function sum(firstNum, secondNum){
    return firstNum + secondNum;
}

let num1 = 10;
let num2 = 20;

// sumResult = sum(num1, num2);
sumResult = sum(10, 20);

console.log(`O valor total da soma de 10 + 20 é: ${sumResult}`)
