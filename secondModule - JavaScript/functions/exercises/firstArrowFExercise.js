const triangleArea = (height, widht) => height * widht / 2;

let triangleAreaResult = triangleArea(10, 20);

console.log(`A área do triangulo com base de 10, e altura de 20 é: ${triangleAreaResult}`);